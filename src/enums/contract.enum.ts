export enum DepositStatus {
    PENDING = "PENDING",
    PAID = "PAID",
    REFUNDED = "REFUNDED",
    DEDUCTED = "DEDUCTED",
    CANCELLED = "CANCELLED",
}

export enum ContractStatus {
    PENDING = "PENDING",
    ACTIVE = "ACTIVE",
    INACTIVE = "INACTIVE",
    CANCELLED = "CANCELLED",
    TERMINATED = "TERMINATED",
}
