enum EquipmentStatus {
    NORMAL = "NORMAL",
    BROKEN = "BROKEN",
    REPAIRING = "REPAIRING",
    DISPOSED = "DISPOSED",
}

enum EquipmentType {
    HOUSE = "HOUSE",
    ROOM = "ROOM",
}

export { EquipmentStatus, EquipmentType };
