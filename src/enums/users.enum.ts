enum AccountRoles {
    ADMIN = "admin",
    USER = "user",
}

enum AccountTypes {
    FREE = "free",
    STANDARD = "standard",
    PREMIUM = "premium",
    ENTERPRISE = "enterprise",
}

export { AccountRoles, AccountTypes };
