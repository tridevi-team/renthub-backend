export enum NotificationType {
    SYSTEM = "system",
    WARNING = "warning",
    REMINDER = "reminder",
    PROMOTION = "promotion",
    UPDATE = "update",
    ALERT = "alert",
}

export enum NotificationStatus {
    UNREAD = "unread",
    READ = "read",
    ARCHIVED = "archived",
}
